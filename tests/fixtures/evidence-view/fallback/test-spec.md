# Test Spec: Fallback Evidence Target

## Testing What

Fallback expectation is visible when a structured quality map is unavailable.

## Evidence Strategy

Use `pnpm test -- tests/contract/evidence-view.contract.test.ts` and
`tests/e2e/evidence-view.spec.ts` as parsed Markdown fallback evidence hints.
