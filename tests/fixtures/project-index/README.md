# Project Index Fixtures

Project index tests compose structured quality-map and Markdown fallback
fixtures from `tests/fixtures/quality-map` and `tests/fixtures/markdown-fallback`
so feature 004 exercises the same upstream data contracts used by features 002
and 003.
