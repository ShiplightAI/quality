# Test Spec: Markdown Only Target

## Testing What

- Users can inspect quality evidence from Markdown artifacts.
- Fallback data remains visibly distinct from structured maps.

## Evidence Strategy

Run `pnpm test` for contract and integration evidence.

## Test Cases

- `tests/contract/markdown-fallback.contract.test.ts`
- `tests/integration/markdown-fallback.test.ts`

## Fixtures And Environments

Static fixtures live in `tests/fixtures/markdown-fallback`.

## Report Expectations

Reports list command outcomes and residual risks.

## Coverage Notes

Coverage is local fixture based.
