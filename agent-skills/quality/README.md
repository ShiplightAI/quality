# Quality agent skill

This directory will contain the public `quality` agent skill and its references.

The skill constructs and maintains `.quality/` artifacts, delegates validation
and scoring to the installed `@shiplightai/quality-tools` version, and preserves
human-only ratification gates.
