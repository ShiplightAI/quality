# `@shiplightai/quality-map`

Internal workspace package for the quality-map contract, parser, validator,
normalizer, diagnostics, vocabularies, and generated JSON Schema.

It is bundled into `@shiplightai/quality-tools` rather than published
independently.
